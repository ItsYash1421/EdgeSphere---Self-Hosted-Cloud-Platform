apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard
  namespace: edgesphere
  labels:
    app: dashboard
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: dashboard
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3100"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: dashboard
        image: ghcr.io/your-org/edgesphere-dashboard:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3100
          name: http
        envFrom:
        - configMapRef:
            name: edgesphere-config
        - secretRef:
            name: edgesphere-secrets
        resources:
          requests: { cpu: 200m, memory: 256Mi }
          limits: { cpu: 1000m, memory: 512Mi }
        readinessProbe:
          httpGet: { path: /health, port: 3100 }
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet: { path: /health, port: 3100 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: edgesphere
  labels: { app: dashboard }
spec:
  selector: { app: dashboard }
  ports:
  - port: 3100
    targetPort: 3100
    name: http
