apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: edgesphere
  labels:
    app: analytics-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: analytics-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3003"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: analytics-service
        image: ghcr.io/your-org/edgesphere-analytics-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
          name: http
        envFrom:
        - configMapRef:
            name: edgesphere-config
        - secretRef:
            name: edgesphere-secrets
        resources:
          requests: { cpu: 200m, memory: 256Mi }
          limits: { cpu: 1000m, memory: 512Mi }
        readinessProbe:
          httpGet: { path: /analytics/health, port: 3003 }
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet: { path: /analytics/health, port: 3003 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: edgesphere
  labels: { app: analytics-service }
spec:
  selector: { app: analytics-service }
  ports:
  - port: 3003
    targetPort: 3003
    name: http
