apiVersion: apps/v1
kind: Deployment
metadata:
  name: websocket-gateway
  namespace: edgesphere
  labels:
    app: websocket-gateway
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: websocket-gateway
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: websocket-gateway
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3006"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: websocket-gateway
        image: ghcr.io/your-org/edgesphere-websocket-gateway:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3006
          name: http
        envFrom:
        - configMapRef:
            name: edgesphere-config
        - secretRef:
            name: edgesphere-secrets
        resources:
          requests: { cpu: 200m, memory: 256Mi }
          limits: { cpu: 1000m, memory: 512Mi }
        readinessProbe:
          httpGet: { path: /health, port: 3006 }
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet: { path: /health, port: 3006 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: websocket-gateway
  namespace: edgesphere
  labels: { app: websocket-gateway }
spec:
  selector: { app: websocket-gateway }
  ports:
  - port: 3006
    targetPort: 3006
    name: http
