apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-service
  namespace: edgesphere
  labels:
    app: cache-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cache-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: cache-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: cache-service
        image: ghcr.io/your-org/edgesphere-cache-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
          name: http
        envFrom:
        - configMapRef:
            name: edgesphere-config
        - secretRef:
            name: edgesphere-secrets
        resources:
          requests: { cpu: 200m, memory: 256Mi }
          limits: { cpu: 1000m, memory: 512Mi }
        readinessProbe:
          httpGet: { path: /health, port: 3004 }
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        livenessProbe:
          httpGet: { path: /health, port: 3004 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sh", "-c", "sleep 5"]
---
apiVersion: v1
kind: Service
metadata:
  name: cache-service
  namespace: edgesphere
  labels: { app: cache-service }
spec:
  selector: { app: cache-service }
  ports:
  - port: 3004
    targetPort: 3004
    name: http
